/**
 * Implements data structure for a 3D vector (x, y, z).
 */
#ifndef SRC_UTIL_VECTOR3_H_
#define SRC_UTIL_VECTOR3_H_

/**
 * Data structure with (x, y, z) using the type float.
 */
typedef struct {
    float x;
    float y;
    float z;
} vector3_t;

#endif
